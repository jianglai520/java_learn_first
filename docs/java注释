java里面有3种注释，全都在写在源码里面给人看，编译时会被完全丢弃，不影响字节码，也不影响运行速度

1.单行注释//
从//到本行末尾都是注释

2.多行注释/* ... */
从/*到最近的*/之间所有的内容都是注释，可以跨很多行。

3.文档注释/** ... */
比多行注释多一个*。它不只是给编译器看的，能被javadoc工具提取出来生成HTML帮助文档，也是IDEA里鼠标悬停时弹出的提示内容